/**
 * window.btoa
 */
declare function btoa(input: string): string;
/**
 * window.atob
 */
declare function atob(input: string): string;
export interface Base64Result {
    string: string | ArrayBuffer | null;
    format: 'data_url' | 'base64';
}
/**
 * Converts a Blob, ArrayBuffer or Uint8Array to a base64 string.
 */
declare function fromData(data: Blob | ArrayBuffer | Uint8Array): Promise<Base64Result>;
declare const _default: {
    btoa: typeof btoa;
    atob: typeof atob;
    fromData: typeof fromData;
};
export default _default;
//# sourceMappingURL=Base64.d.ts.map